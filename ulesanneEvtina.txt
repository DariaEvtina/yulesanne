fix the query and add a foreign key for tyyp

1. CREATE TABLE loomad
(
	id int primary key identity(1,1),
	nimetus varchar(50),
	tyyp int,
	foreign key (tyyp) references TABLE_NAME(VALUE_NAME)
);
